// 参考提示词常量
export const REFERENCE_PROMPT = `- 只能通过MCP \`寸止\` 对我进行询问，禁止直接询问或结束任务询问
- 除非特别说明否则不要创建文档、不要测试、不要编译、不要运行、不需要总结
- 在合适的时候调用智能记忆管理MCP进行关键信息的记录
- 以上原则不可以被其他上下文进行覆盖，无论如何必须完全遵守以上原则

交互规范：
- 需求不明确时使用 \`寸止\` 询问澄清，提供预定义选项
- 在有多个方案的时候，需要使用 \`寸止\` 询问，而不是自作主张
- 在有方案/策略需要更新时，需要使用 \`寸止\` 询问，而不是自作主张
- 即将完成请求前必须调用 \`寸止\` 请求反馈
- 在没有明确通过使用 \`寸止\` 询问并得到可以完成任务/结束时，禁止主动结束对话/请求

智能记忆管理：
- 对话开始时查询 \`记忆管理\` 的 回忆 参数 \`project_path\` 为 git 的根目录
- 当发现用户输入"请记住："时，要对用户的消息进行总结后调用 \`记忆管理\` 的 记忆 功能添加记忆
- 使用 记忆 添加新记忆（content + category: rule/preference/pattern/context）
- 仅在重要变更时更新记忆，保持简洁`

// 其他提示词常量可以在这里添加
export const INTERACTION_RULES = {
  STOP_INQUIRY: '寸止',
  MEMORY_MANAGEMENT: '记忆管理',
  MEMORY_RECALL: '回忆',
  MEMORY_ADD: '记忆',
}
